# ==== Подключаем модули ====  
кинуть "легавый" из станка  # модуль валидации  
кинуть "хаза" из станка     # база данных  
кинуть "топор" из станка    # утилиты для работы со строками  

# ==== Сущность "Бык" (User) ====  
тип Бык {  
    паспорт: число @ключ,  
    кличка: строка @ментам(мин=3, макс=20),  # валидация через "ментов"  
    бабки: число @по_умолчанию(0),  
    тюрьма: логическое @скрытый              # приватное поле  
}  

# ==== CRUD операции ====  

# 🔍 **Чтение (R — "Рвать")**  
функция грянуть_базар() -> тусовка[Бык]:  
    """Стрельнуть всех быков из хазы."""  
    вернуть хаза.наезжать("быки")  

функция найти_быка(паспорт: число) -> Бык:  
    бык = хаза.шмонать("быки", паспорт)  
    если не бык:  
        кинуть_разборки!("Бык с паспортом #{паспорт} не найден!")  
    вернуть бык  

# ➕ **Создание (C — "Чистить")**  
функция поставить_на_счетчик(кличка: строка, бабки: число) -> Бык:  
    """Лепим нового быка и ставим на учёт."""  
    если легавый.прижучить(кличка, "кличка"):  # валидация  
        новобранец = Бык(кличка=кличка, бабки=бабки)  
        хаза.запихнуть("быки", новобранец)  
        вернуть новобранец  
    иначе:  
        кинуть_разборки!("Кличка не прошла проверку ментов!")  

# ✏️ **Обновление (U — "Утрясти")**  
функция переписать_паспорт(паспорт: число, новые_данные: мешок) -> Бык:  
    """Меняем данные быка (например, после "договорняка")."""  
    старый_бык = найти_быка(паспорт)  
    новый_бык = {**старый_бык, **новые_данные}  
    хаза.перетряхнуть("быки", паспорт, новый_бык)  
    вернуть новый_бык  

# 🗑️ **Удаление (D — "Душить")**  
функция уйти_в_закат(паспорт: число) -> строка:  
    """Стираем быка из базы — "отходим от дел"."""  
    хаза.замести_следы("быки", паспорт)  
    вернуть "Бык #{паспорт} больше не светится"  

# ==== Пример использования ====  
функция главная():  
    # Ставим быка на счётчик  
    новичок = поставить_на_счетчик("Косой", 5000)  
    печать(f"Новый бык: {новичок.кличка}, бабки: {новичок.бабки}")  

    # Подкручиваем бабки (типа "договорняк")  
    переписать_паспорт(1, {"бабки": 10000})  
    печать("Бабки подкручены!")  

    # Уходим в закат  
    печать(уйти_в_закат(1))  

# ==== Глобальная область (общак) ====  
общак {  
    легавые_на_страже: логическое = нет,   # флаг безопасности  
    чёрная_касса: мешок = {},              # кеш  
    ментовской_протокол: строка = "https://ментовской.база"  
}  

# ==== Функция для работы с общаком ====  
функция закинуть_в_общак(ключ: строка, значение: любое):  
    """Кладём значение в общак (типа global state)."""  
    общак[ключ] = значение  

функция взять_из_общака(ключ: строка) -> любое:  
    """Достаём что-то из общака."""  
    вернуть общак.грести(ключ)  
