# ==== Настройки базы "хазы" (от "хардкорная база") ====  
подключить "хаза" из станка  # станок = стандартная библиотека  

# ==== Сущность "Бык" (User) ====  
тип Бык {  
    паспорт: число @ключ,          # id  
    кличка: строка @наводка,       # name (валидация)  
    бабки: число @по_умолчанию(0)  # money  
}  

# ==== CRUD-операции ====  

# 🔍 Чтение (R)  
функция стрельнуть_быков() -> список[Бык]:  
    вернуть хаза.грести("быки")  # SELECT *  

функция найти_быка(паспорт: число) -> Бык:  
    бык = хаза.копать("быки", паспорт)  
    если не бык:  
        базар_на_копыто!("Быка с паспортом #{паспорт} нет!")  # throw error  
    вернуть бык  

# ➕ Создание (C)  
функция слепить_быка(кличка: строка, бабки: число) -> Бык:  
    новенький = Бык(кличка=кличка, бабки=бабки)  
    хаза.впихнуть("быки", новенький)  # INSERT  
    вернуть новенький  

# ✏️ Обновление (U)  
функция перебить_паспорт(паспорт: число, новые_данные: словарь) -> Бык:  
    бык = найти_быка(паспорт)  
    перепачканный = {**бык, **новые_данные}  # "Подчищаем следы"  
    хаза.переписать("быки", паспорт, перепачканный)  # UPDATE  
    вернуть перепачканный  

# 🗑️ Удаление (D)  
функция закопать_быка(паспорт: число) -> логическое:  
    хаза.слить("быки", паспорт)  # DELETE  
    вернуть да  # "Документы уничтожены"  

# === Пример использования ===  
главная():  
    слепить_быка("Косой", 5000)  
    найденный = найти_быка(1)  
    печать(f"Новый бык: {найденный.кличка}, бабки: {найденный.бабки}")  

    перебить_паспорт(1, {"бабки": 10000})  # "Добавили лавешников"  
    закопать_быка(1)  # "Вынеслен на опушку" 
