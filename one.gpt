# ==== Подключаем модули ====  
кинуть "легавый" из станка  
кинуть "хаза" из станка  
кинуть "чёрная_касса" из станка  # модуль кеширования  

# ==== Сущность "Бык" ====  
тип Бык {  
    паспорт: число @ключ,  
    кличка: строка @ментам(мин=3, макс=20),  
    рейтинг: число @по_умолчанию(0),  
    в_розыске: логическое @скрытый  
}  

# ==== CRUD с кешем и общаком ====  

# 🔍 Чтение с кешем  
функция грянуть_быков() -> тусовка[Бык]:  
    """Если в чёрной кассе есть данные — берём оттуда."""  
    если общак.легавые_на_страже:  
        печать("Внимание! Менты следят за базой!")  
      
    кэшированные = чёрная_касса.грести("быки")  
    если кэшированные:  
        вернуть кэшированные  
    иначе:  
        данные = хаза.наезжать("быки")  
        чёрная_касса.запихнуть("быки", данные)  
        вернуть данные  

# ➕ Создание с общаком  
функция поставить_на_счетчик(кличка: строка) -> Бык:  
    """Добавляем быка и обновляем общак."""  
    новобранец = Бык(кличка=кличка)  
    хаза.запихнуть("быки", новобранец)  
    закинуть_в_общак("последний_бык", новобранец)  # сохраняем в общак  
    вернуть новобранец  

# ==== Работа с ментовским протоколом (HTTP) ====  
функция ментовской_запрос(урл: строка) -> мешок:  
    """Стреляем запросом к ментам (API)."""  
    если общак.легавые_на_страже:  
        кинуть_разборки!("Менты бдят! Запрос отменён.")  
    ответ = ментовской_протокол.стрелять(урл)  
    вернуть ответ.разбирать()  # парсим JSON  

# ==== Пример использования ====  
главная():  
    # Включаем ментовской режим  
    общак.легавые_на_страже = да  

    # Ставим быка на счётчик  
    бык = поставить_на_счетчик("Гроза")  
    печать(f"Новый бык: {бык.кличка}")  

    # Делаем запрос к ментам (если не заблокировано)  
    попробуй:  
        данные = ментовской_запрос("/проверка/1")  
        печать(f"Данные ментов: {данные}")  
    поймать_разборки как ошибка:  
        печать(f"Менты не пустили: {ошибка}")  
