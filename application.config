# Включаем режим отладки
чистосердечка = да

# Создаем тестового быка
фанерный_бык = подсунуть_фанеру()
проверить_налохи(фанерный_бык)  # шмон покажет все поля

# Запускаем фоновую проверку
отправить_на_разборки(999)

# Пытаемся удалить (но в режиме отладки только логируем)
замочить_насовсем(999)  # напишет в лог, но не удалит
